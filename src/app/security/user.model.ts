import { StringMap } from "@angular/compiler/src/compiler_facade_interface";

export interface User {
  name: string;
  lastName: string;
  username: string;
  email: string;
  userId: string;
  password: string;
}
